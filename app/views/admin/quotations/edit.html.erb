<% content_for :page_title, "見積編集" %>

<div class="space-y-4">
  <div>
    <h1 class="text-xl font-semibold text-slate-900">見積を編集</h1>
    <p class="text-sm text-slate-600"><%= @quotation.quotation_number %> の下書きを更新します。</p>
  </div>

  <div class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm">
    <%= render "form", quotation: @quotation %>
  </div>
</div>
