<% content_for :page_title, "請求締め一覧" %>

<div class="space-y-4">
  <div class="flex items-center justify-between gap-3">
    <div>
      <h1 class="text-xl font-semibold text-slate-900">請求締め一覧</h1>
      <p class="text-sm text-slate-600">月次請求の締め実行履歴です。</p>
    </div>
    <%= link_to "請求一覧へ", admin_invoices_path, class: "rounded border border-slate-200 bg-white px-3 py-2 text-sm text-slate-700 hover:bg-slate-50" %>
  </div>

  <% if @billing_batches.any? %>
    <div class="overflow-x-auto rounded-xl border border-slate-200 bg-white shadow-sm">
      <table class="min-w-full text-left text-sm">
        <thead class="bg-slate-50 text-xs uppercase tracking-wide text-slate-500">
          <tr>
            <th class="px-4 py-2">締め番号</th>
            <th class="px-4 py-2">締日</th>
            <th class="px-4 py-2">請求期間</th>
            <th class="px-4 py-2">請求日</th>
            <th class="px-4 py-2">請求件数</th>
            <th class="px-4 py-2">金額</th>
            <th class="px-4 py-2">状態</th>
            <th class="px-4 py-2 text-right">操作</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-slate-100">
          <% @billing_batches.each do |billing_batch| %>
            <tr class="hover:bg-slate-50">
              <td class="px-4 py-2 font-semibold text-slate-900"><%= billing_batch.batch_number %></td>
              <td class="px-4 py-2 text-slate-700"><%= jp_date(billing_batch.closing_date) %></td>
              <td class="px-4 py-2 text-slate-700"><%= jp_date(billing_batch.billing_period_from) %> 〜 <%= jp_date(billing_batch.billing_period_to) %></td>
              <td class="px-4 py-2 text-slate-700"><%= jp_date(billing_batch.invoice_date) %></td>
              <td class="px-4 py-2 text-slate-700"><%= billing_batch.invoice_count %></td>
              <td class="px-4 py-2 text-slate-700"><%= money(billing_batch.total_amount) %></td>
              <td class="px-4 py-2"><%= billing_batch_status_badge(billing_batch) %></td>
              <td class="px-4 py-2 text-right">
                <%= link_to "詳細", admin_billing_batch_path(billing_batch), class: "rounded border border-slate-200 bg-white px-3 py-1.5 text-xs text-slate-700 hover:bg-slate-50" %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
      <%= render "shared/pagy", pagy: @pagy %>
    </div>
  <% else %>
    <div class="rounded-lg border border-dashed border-slate-300 bg-slate-50 p-6 text-center text-slate-600">
      <p>請求締めはまだ実行されていません。</p>
    </div>
  <% end %>
</div>
