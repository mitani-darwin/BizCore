<% content_for :page_title, "在庫作成" %>

<div class="space-y-4">
  <div>
    <h1 class="text-xl font-semibold text-slate-900">在庫を登録</h1>
    <p class="text-sm text-slate-600">倉庫と商品の組み合わせごとに在庫を持ちます。</p>
  </div>

  <div class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm">
    <%= render "form", stock_item: @stock_item %>
  </div>
</div>
