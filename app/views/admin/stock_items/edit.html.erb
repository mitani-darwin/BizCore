<% content_for :page_title, "在庫編集" %>

<div class="space-y-4">
  <div>
    <h1 class="text-xl font-semibold text-slate-900">在庫設定を編集</h1>
    <p class="text-sm text-slate-600"><%= @stock_item.warehouse.name %> / <%= @stock_item.product.name %> の安全在庫を更新します。</p>
  </div>

  <div class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm">
    <%= render "form", stock_item: @stock_item %>
  </div>
</div>
