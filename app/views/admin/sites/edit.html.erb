<% content_for :page_title, "現場編集" %>

<div class="space-y-4">
  <div>
    <h1 class="text-xl font-semibold text-slate-900">現場編集</h1>
    <p class="text-sm text-slate-600"><%= @site.name %> の情報を編集します。</p>
  </div>

  <div class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm">
    <%= render "form", site: @site %>
  </div>
</div>
