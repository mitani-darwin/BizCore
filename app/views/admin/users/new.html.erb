<div class="mx-auto max-w-3xl px-4 py-6 sm:px-6 lg:px-8">
  <div class="mb-6 flex items-center justify-between">
    <div>
      <h1 class="text-xl font-semibold text-slate-900">ユーザ追加</h1>
      <p class="mt-1 text-xs text-slate-500">テナントに紐づく新規ユーザを作成します。</p>
    </div>
    <%= link_to "← ユーザ一覧へ戻る",
        admin_users_path,
        class: "text-xs font-medium text-slate-600 hover:text-slate-800" %>
  </div>

  <div class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm">
    <%= form_with model: @user, url: admin_users_path, local: true, class: "space-y-4" do |f| %>
      <div class="grid gap-4 sm:grid-cols-2">
        <div class="sm:col-span-2">
          <label class="mb-1 block text-[11px] font-medium text-slate-600">テナント</label>
          <% if @selected_tenant.present? %>
            <div class="flex items-center justify-between rounded-lg border border-slate-200 bg-slate-50 px-3 py-2">
              <div>
                <p class="text-sm font-semibold text-slate-900"><%= @selected_tenant.name %></p>
                <p class="text-[11px] text-slate-600"><%= @selected_tenant.subdomain %> / <%= @selected_tenant.code %></p>
              </div>
              <span class="text-[11px] text-slate-500">変更不可</span>
            </div>
            <%= f.hidden_field :tenant_id, value: @selected_tenant.id %>
          <% else %>
            <div class="rounded-lg border border-amber-200 bg-amber-50 px-3 py-2 text-sm text-amber-800">
              テナントが未選択です。ユーザ一覧でテナントを選択してからユーザ追加に進んでください。
            </div>
          <% end %>
        </div>

        <div class="sm:col-span-2">
          <label class="mb-1 block text-[11px] font-medium text-slate-600">ロール</label>
          <% if @selected_tenant.blank? %>
            <div class="rounded-lg border border-amber-200 bg-amber-50 px-3 py-2 text-sm text-amber-800">
              テナント選択後にロールを設定できます。
            </div>
          <% elsif @role_options.empty? %>
            <div class="rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-sm text-slate-600">
              このテナントにロールがありません。先にロールを作成してください。
            </div>
          <% else %>
            <div class="grid gap-2 sm:grid-cols-2">
              <% @role_options.each do |role| %>
                <label class="flex items-center gap-2 rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm text-slate-800 hover:bg-slate-50">
                  <%= check_box_tag "user[role_ids][]", role.id, @user.roles.include?(role),
                      class: "h-4 w-4 rounded border-slate-300 text-sky-600 focus:ring-sky-500" %>
                  <span class="text-sm font-medium"><%= role.name %></span>
                  <span class="text-[11px] text-slate-500"><%= role.description.presence || role.key %></span>
                </label>
              <% end %>
            </div>
          <% end %>
        </div>

        <div>
          <label class="mb-1 block text-[11px] font-medium text-slate-600">氏名</label>
          <%= f.text_field :name,
              class: "w-full rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm text-slate-800 focus:border-sky-500 focus:outline-none focus:ring-1 focus:ring-sky-500",
              placeholder: "山田 太郎" %>
        </div>

        <div>
          <label class="mb-1 block text-[11px] font-medium text-slate-600">メールアドレス</label>
          <%= f.email_field :email,
              class: "w-full rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm text-slate-800 focus:border-sky-500 focus:outline-none focus:ring-1 focus:ring-sky-500",
              placeholder: "user@example.com" %>
        </div>

        <div>
          <label class="mb-1 block text-[11px] font-medium text-slate-600">パスワード</label>
          <%= f.password_field :password,
              class: "w-full rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm text-slate-800 focus:border-sky-500 focus:outline-none focus:ring-1 focus:ring-sky-500" %>
        </div>

        <div>
          <label class="mb-1 block text-[11px] font-medium text-slate-600">パスワード（確認）</label>
          <%= f.password_field :password_confirmation,
              class: "w-full rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm text-slate-800 focus:border-sky-500 focus:outline-none focus:ring-1 focus:ring-sky-500" %>
        </div>

        <%= f.hidden_field :time_zone, value: @user.time_zone.presence || "Asia/Tokyo" %>
        <%= f.hidden_field :locale, value: @user.locale.presence || "ja" %>

        <div class="sm:col-span-2">
          <label class="inline-flex items-center gap-2 text-sm text-slate-800">
            <%= f.check_box :is_owner, class: "h-4 w-4 rounded border-slate-300 text-sky-600 focus:ring-sky-500" %>
            <span class="text-[13px]">テナントオーナーとして登録する</span>
          </label>
        </div>
      </div>

      <% if @user.errors.any? %>
        <div class="rounded-lg border border-rose-200 bg-rose-50 p-3 text-sm text-rose-700">
          <p class="font-semibold">入力内容を確認してください。</p>
          <ul class="mt-1 list-disc pl-5">
            <% @user.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="flex items-center gap-2">
        <%= link_to "キャンセル", admin_users_path,
            class: "inline-flex items-center rounded-lg border border-slate-200 bg-white px-4 py-2 text-sm font-medium text-slate-700 hover:bg-slate-50" %>
        <%= f.submit "ユーザを作成",
            class: "inline-flex items-center rounded-lg bg-sky-600 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-sky-700" %>
      </div>
    <% end %>
  </div>
</div>
