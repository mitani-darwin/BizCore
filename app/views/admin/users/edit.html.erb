<% content_for :page_title do %>
  ユーザ編集
<% end %>

<div class="space-y-4">
  <div>
    <h1 class="text-xl font-semibold text-slate-900">ユーザを編集</h1>
    <p class="text-sm text-slate-600"><%= @user.name %> の情報を更新します。</p>
  </div>

  <div class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm">
    <%= render "form", user: @user %>
  </div>
</div>
