<% content_for :page_title, "発注編集" %>

<div class="space-y-4">
  <div>
    <h1 class="text-xl font-semibold text-slate-900">発注を編集</h1>
    <p class="text-sm text-slate-600"><%= @purchase_order.purchase_order_number %> の下書きを更新します。</p>
  </div>

  <div class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm">
    <%= render "form", purchase_order: @purchase_order %>
  </div>
</div>
