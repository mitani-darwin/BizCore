<% content_for :page_title do %>
  テナント詳細
<% end %>

<div class="space-y-4">
  <div class="flex items-center justify-between gap-3">
    <div>
      <h1 class="text-xl font-semibold text-slate-900"><%= @tenant.name %></h1>
      <p class="text-sm text-slate-600">テナントの基本情報です。</p>
    </div>
    <% if can?("admin.tenants.update") %>
      <%= link_to "編集", edit_admin_tenant_path(@tenant), class: "rounded-lg bg-sky-600 px-4 py-2 text-sm font-semibold text-white hover:bg-sky-700" %>
    <% end %>
  </div>

  <div class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm">
    <dl class="grid grid-cols-1 gap-4 sm:grid-cols-2">
      <div class="rounded-lg bg-slate-50 px-4 py-3">
        <dt class="text-xs text-slate-500">コード</dt>
        <dd class="text-sm font-semibold text-slate-900"><%= @tenant.code %></dd>
      </div>
      <div class="rounded-lg bg-slate-50 px-4 py-3">
        <dt class="text-xs text-slate-500">サブドメイン</dt>
        <dd class="text-sm font-semibold text-slate-900"><%= @tenant.subdomain %></dd>
      </div>
      <div class="rounded-lg bg-slate-50 px-4 py-3">
        <dt class="text-xs text-slate-500">プラン</dt>
        <dd class="text-sm font-semibold text-slate-900"><%= @tenant.plan %></dd>
      </div>
      <div class="rounded-lg bg-slate-50 px-4 py-3">
        <dt class="text-xs text-slate-500">ステータス</dt>
        <dd class="text-sm font-semibold text-slate-900"><%= @tenant.status %></dd>
      </div>
      <div class="rounded-lg bg-slate-50 px-4 py-3 sm:col-span-2">
        <dt class="text-xs text-slate-500">請求メール</dt>
        <dd class="text-sm font-semibold text-slate-900 break-all"><%= @tenant.billing_email %></dd>
      </div>
      <div class="rounded-lg bg-slate-50 px-4 py-3">
        <dt class="text-xs text-slate-500">請求締め日</dt>
        <dd class="text-sm font-semibold text-slate-900">
          <% if @tenant.billing_closing_day.present? && @tenant.billing_closing_day.to_i > 0 %>
            <%= @tenant.billing_closing_day %>日
          <% else %>
            末日
          <% end %>
        </dd>
      </div>
      <div class="rounded-lg bg-slate-50 px-4 py-3">
        <dt class="text-xs text-slate-500">給与締め日</dt>
        <dd class="text-sm font-semibold text-slate-900">
          <% if @tenant.payroll_closing_day.present? && @tenant.payroll_closing_day.to_i > 0 %>
            <%= @tenant.payroll_closing_day %>日
          <% else %>
            末日
          <% end %>
        </dd>
      </div>
      <div class="rounded-lg bg-slate-50 px-4 py-3">
        <dt class="text-xs text-slate-500">仕入締め日</dt>
        <dd class="text-sm font-semibold text-slate-900">
          <% if @tenant.purchase_closing_day.present? && @tenant.purchase_closing_day.to_i > 0 %>
            <%= @tenant.purchase_closing_day %>日
          <% else %>
            末日
          <% end %>
        </dd>
      </div>
    </dl>
  </div>

  <div class="flex gap-2">
    <%= link_to "一覧に戻る", admin_tenants_path, class: "rounded border border-slate-200 bg-white px-3 py-2 text-sm text-slate-700 hover:bg-slate-50" %>
    <% if can?("admin.tenants.update") %>
      <%= link_to "編集", edit_admin_tenant_path(@tenant), class: "rounded border border-sky-200 bg-sky-50 px-3 py-2 text-sm font-semibold text-sky-700 hover:bg-sky-100" %>
    <% end %>
  </div>
</div>
