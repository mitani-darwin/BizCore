<% content_for :page_title, "従業員編集" %>

<div class="space-y-4">
  <div>
    <h1 class="text-xl font-semibold text-slate-900">従業員編集</h1>
    <p class="text-sm text-slate-600"><%= @employee.employee_code %> <%= @employee.name %> の設定を更新します。</p>
  </div>

  <div class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm">
    <%= render "form", employee: @employee %>
  </div>
</div>
