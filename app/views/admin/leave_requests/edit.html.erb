<% content_for :page_title, "有給編集" %>

<div class="space-y-4">
  <div>
    <h1 class="text-xl font-semibold text-slate-900">有給編集</h1>
    <p class="text-sm text-slate-600"><%= @leave_request.employee.name %> の申請内容を更新します。</p>
  </div>

  <div class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm">
    <%= render "form", leave_request: @leave_request %>
  </div>
</div>
