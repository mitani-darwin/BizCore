<% content_for :page_title, "得意先編集" %>

<div class="space-y-4">
  <div>
    <h1 class="text-xl font-semibold text-slate-900">得意先を編集</h1>
    <p class="text-sm text-slate-600"><%= @customer.name %> の取引条件を更新します。</p>
  </div>

  <div class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm">
    <%= render "form", customer: @customer %>
  </div>
</div>
