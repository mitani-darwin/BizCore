<% content_for :page_title do %>
  ロール一覧
<% end %>

<div class="space-y-4">
  <div class="flex items-center justify-between gap-3">
    <div>
      <h1 class="text-xl font-semibold text-slate-900">ロール一覧</h1>
      <p class="text-sm text-slate-600">現在のテナントに紐づくロールのみ表示します。</p>
    </div>
    <% if can?("admin.roles.create") %>
      <%= link_to "新規ロール", new_admin_role_path, class: "rounded-lg bg-sky-600 px-4 py-2 text-sm font-semibold text-white hover:bg-sky-700" %>
    <% end %>
  </div>

  <% if @roles.any? %>
    <div class="space-y-3">
      <% @roles.each do |role| %>
        <div class="rounded-xl border border-slate-200 bg-white p-4 shadow-sm">
          <div class="flex items-start justify-between gap-3">
            <div>
              <div class="flex items-center gap-2">
                <h2 class="text-lg font-semibold text-slate-900"><%= role.name %></h2>
                <% if role.built_in? %>
                  <span class="rounded-full bg-slate-100 px-2 py-0.5 text-xs font-semibold text-slate-700">built-in</span>
                <% end %>
              </div>
              <p class="text-sm text-slate-600">キー: <%= role.key %></p>
            </div>
            <div class="flex gap-2">
              <% if can?("admin.roles.read") %>
                <%= link_to "詳細", admin_role_path(role), class: "rounded border border-slate-200 bg-white px-3 py-1.5 text-sm text-slate-700 hover:bg-slate-50" %>
              <% end %>
              <% if can?("admin.roles.update") %>
                <%= link_to "編集", edit_admin_role_path(role), class: "rounded border border-sky-200 bg-sky-50 px-3 py-1.5 text-sm font-semibold text-sky-700 hover:bg-sky-100" %>
              <% end %>
            </div>
          </div>

          <div class="mt-3 text-sm text-slate-700">
            <div class="font-semibold text-slate-800">付与権限</div>
            <% if role.permissions.any? %>
              <div class="mt-2 flex flex-wrap gap-2">
                <% role.permissions.each do |permission| %>
                  <span class="rounded-full bg-slate-50 px-3 py-1 text-xs text-slate-700 border border-slate-200"><%= permission.key %></span>
                <% end %>
              </div>
            <% else %>
              <p class="mt-2 text-xs text-slate-500">権限がまだ設定されていません。</p>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="rounded-lg border border-dashed border-slate-300 bg-slate-50 p-6 text-center text-slate-600">
      <p>ロールがありません。まずは作成してください。</p>
      <% if can?("admin.roles.create") %>
        <div class="mt-3">
          <%= link_to "新規ロールを作成", new_admin_role_path, class: "inline-flex items-center rounded-lg bg-sky-600 px-4 py-2 text-sm font-semibold text-white hover:bg-sky-700" %>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
