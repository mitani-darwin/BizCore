<% content_for :page_title, "契約新規作成" %>
<div class="space-y-4">
  <div>
    <h1 class="text-xl font-semibold text-slate-900">契約新規作成</h1>
    <p class="text-sm text-slate-600">得意先・仕入先との契約を登録します。</p>
  </div>
  <div class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm">
    <%= render "form", contract: @contract %>
  </div>
</div>
