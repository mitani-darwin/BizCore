<% content_for :page_title, "契約編集" %>
<div class="space-y-4">
  <div>
    <h1 class="text-xl font-semibold text-slate-900">契約編集</h1>
    <p class="text-sm text-slate-600"><%= @contract.contract_number %> / <%= @contract.title %></p>
  </div>
  <div class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm">
    <%= render "form", contract: @contract %>
  </div>
</div>
